STRUCTURE :

tempo = ...
tuning = ...    [ optional , default = standard ]
capo = ...      [ optional , default = 0 ]

stream <stream_name> ():
    note ()
    notes ()
    chord ()
    skip

add_stream (<stream_name>)

compose ()